export type Document = {
  title: string;
  abstract: string;
  year: number;
  url: string;
};

export type SelectorItem = {
  name: string;
};

export type SelectedField = {
  label: string;
  value: number;
};
